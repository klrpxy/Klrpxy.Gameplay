# Support Unity 2022 LTS

The first release will support Unity 2022.3 LTS as its minimum Unity version. The source generator must therefore target .NET Standard 2.0 and remain compatible with the Roslyn 3.8 API and language capabilities used by Unity 2022, while the same package is verified separately on Unity 6.
